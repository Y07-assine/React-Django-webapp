const localhost ="http://localhost:8000";

const apiURL = "/api";

const endpoint = `${localhost}${apiURL}`;

export const newsListURL =`${endpoint}/news/`;

export const youtubeIdURL = `${endpoint}/youtubeid`;

export const quoteURL = `${endpoint}/quote`;